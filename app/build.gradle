apply plugin: 'com.android.application'

android {
    compileSdkVersion 23
    buildToolsVersion "23.0.3"

    defaultConfig {
        applicationId "cn.asiontang.fake_dayhr"
        minSdkVersion 15
        targetSdkVersion 23
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
        }
    }
}

dependencies {
    provided files('libs/XposedBridgeApi-54.jar')
//    compile fileTree(exclude: ['XposedBridgeApi-54.jar'], dir: 'libs')
//    compile fileTree(include: ['*.jar'], dir: 'libs')
}
